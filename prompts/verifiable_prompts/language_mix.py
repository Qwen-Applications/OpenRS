language_mix_prompt_template = """
# 角色与定位

你是一位专注于语言匹配性的 “语言规范裁判”。你的任务是根据提供的【Query】，评估【Response】是否存在 “语言不匹配” 的情况。

# 核心评判原则

你只需要判断以下两种 “语言不匹配” 情况：

1. **中英不匹配**：当用户 Query 是**纯中文**（简体或繁体）时，回答不包含中文
   - 翻译任务不考虑中英不匹配
   - 用户 Query 的需求是写英文的情况, 不属于中英不匹配
   - Response 中存在中英混杂, 不属于中英不匹配
2. **简繁不匹配**：当用户 Query 是**繁体中文**时，回答是否使用了简体中文

# 核心任务

1.  **步骤一：识别 Query 语言**
    - 准确判断 Query 的书写语言：**纯英文**、**简体中文**、**繁体中文** 或其他
2.  **步骤二：核查 Response**
    - 根据 Query 的语言，检查 Response 是否存在 “中英不匹配” 或 “简繁不匹配”
3.  **步骤三：裁定与评分**
    - 如果 Response 存在不匹配 Query 语言情况，评分为 **0分**
    - 如果 Response 语言匹配 Query 的语言要求，评分为 **1分**

# 参考信息

<query>
{query}
</query>

<response>
{response}
</response>

# 输出格式要求（严格遵守 JSON 结构）

```json
{{
  "query_language": "明确指出 Query 的语言（如：简体中文、繁体中文、英文等）",
  "analysis": "简要分析 Response 是否存在‘中英不匹配’或‘简繁不匹配’",
  "score": 1 或 0,
}}
```
"""